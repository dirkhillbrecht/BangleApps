// Removal notice now the changes are in the official Anton clock
g.reset();
E.showMessage("Remove \"Anton clock WS\" and install\n\"Anton clock\"\ninstead. Changes have been merged.","Anton clock WS");
setTimeout(load,10000);
// end of file
